apiVersion: v1
kind: Service

metadata:
  name: staff-frontend-service
  namespace: staff-management

spec:
  type: ClusterIP

  selector:
    app: staff-frontend

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80